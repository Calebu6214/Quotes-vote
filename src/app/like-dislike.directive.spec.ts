import { LikeDislikeDirective } from './like-dislike.directive';

describe('LikeDislikeDirective', () => {
  it('should create an instance', () => {
    const directive = new LikeDislikeDirective();
    expect(directive).toBeTruthy();
  });
});
